{
  "name": "@salin/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'No shared tests yet'",
    "generate": "openapi-typescript ../../apps/api/openapi.json -o ./src/generated/api-types.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.8.0",
    "typescript": "^5.6.3"
  }
}
