import { AppShell } from "@/components/app-shell";
import { DemoRecordingWorkspace } from "@/components/demo-recording-workspace";

export default function PreviewRecordingPage() {
  return (
    <AppShell>
      <DemoRecordingWorkspace />
    </AppShell>
  );
}
