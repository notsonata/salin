APP_ENV=development
DATABASE_URL=postgresql+psycopg://salin:salin@postgres:5432/salin
REDIS_URL=redis://redis:6379/0
CORS_ALLOWED_ORIGINS=http://localhost:3000

R2_BUCKET_NAME=salin-dev
R2_ENDPOINT_URL=https://<account-id>.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=changeme
R2_SECRET_ACCESS_KEY=changeme
R2_REGION=auto

GROQ_API_KEY=changeme
GROQ_TRANSCRIPTION_MODEL=whisper-large-v3
GROQ_FAST_MODEL=whisper-large-v3-turbo

OPENROUTER_API_KEY=changeme
OPENROUTER_MODELS=openai/gpt-4.1-mini,anthropic/claude-3.5-haiku

LOCAL_TRANSCRIPTION_MODEL=small
TRANSCRIPTION_CHUNK_MINUTES=10
TRANSCRIPTION_CHUNK_OVERLAP_SECONDS=15
RECORDING_JOB_TIMEOUT_SECONDS=3600
NOTES_JOB_TIMEOUT_SECONDS=600
YOUTUBE_IMPORT_MAX_MINUTES=180
# Optional: set this to a mounted Netscape-format cookies.txt file when YouTube
# asks yt-dlp to confirm the request is not a bot.
YOUTUBE_COOKIES_FILE=
# Optional: use the same browser User-Agent that exported the YouTube cookies.
YOUTUBE_USER_AGENT=
# Optional: production Compose can run a PO-token provider at this URL.
YOUTUBE_POT_PROVIDER_URL=

DIARIZATION_PROVIDER=none
PYANNOTE_AUTH_TOKEN=
PYANNOTE_MODEL=pyannote/speaker-diarization-community-1
# Use auto, cpu, cuda, or mps.
PYANNOTE_DEVICE=auto
PYANNOTE_METRICS_ENABLED=0

MAX_UPLOAD_MB=100

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
SALIN_API_INTERNAL_BASE_URL=http://api:8000
